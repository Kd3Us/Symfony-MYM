<div class="quick-action {{class|default('')}}">
    <span class="icon">
        {% include 'components/Icon.html.twig' with { 'iconName': iconName, "size": "sm" } %}
    </span>
    <span class="text">{{ text|default("Text") }}</span>
</div>
