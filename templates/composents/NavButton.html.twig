<div class="nav-button {% if isActionButton %}nav-button-action{% endif %}">
    <div class="icon">
        {% include 'composents/Icon.html.twig' with { 'selected_icon': iconName, "size": "sm" } %}
    </div>
    <span class="text">{{ text }}</span>
</div>